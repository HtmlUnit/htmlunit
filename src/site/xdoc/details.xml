<?xml version="1.0"?>
<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://maven.apache.org/XDOC/2.0 https://maven.apache.org/xsd/xdoc-2.0.xsd">

    <properties>
        <title>Details about HtmlUnit</title>
        <author>RBRi</author>
    </properties>

    <body>
        <macro name="toc"/>

        <section name="Request headers" id="request_headers">
            <p>
                HtmlUnit mimics the browser as closely as possible, which includes sending request headers.
                You can customize headers at three different levels: Request level, WebClient level, and BrowserVersion level.
            </p>

            <subsection name="BrowserVersion level">
                <p>
                    To change request headers at the BrowserVersion level, create a customized browser version using <code>BrowserVersionBuilder</code>.
                </p>
<source><![CDATA[final BrowserVersion browser =
    new BrowserVersion.BrowserVersionBuilder(BrowserVersion.FIREFOX)
          .setAcceptLanguageHeader("de-CH")
          .build();

final WebClient webClient = new WebClient(browser);
]]></source>
                <p>
                    Methods available to customize basic browser behavior and request headers include:
                </p>
                <ul>
                    <li><code>setApplicationCodeName(String)</code></li>
                    <li><code>setApplicationMinorVersion(String)</code></li>
                    <li><code>setApplicationName(String)</code></li>
                    <li><code>setApplicationVersion(String)</code></li>
                    <li><code>setBuildId(String)</code></li>
                    <li><code>setPlatform(String)</code></li>
                    <li><code>setSystemLanguage(String)</code></li>
                    <li><code>setSystemTimezone(TimeZone)</code></li>
                    <li><code>setUserAgent(String)</code></li>
                    <li><code>setVendor(String)</code></li>
                    <li><code>setUserLanguage(String)</code></li>
                    <li><code>setBrowserLanguage(String)</code></li>
                    <li><code>setAcceptEncodingHeader(String)</code></li>
                    <li><code>setAcceptLanguageHeader(String)</code></li>
                    <li><code>setCssAcceptHeader(String)</code></li>
                    <li><code>setHtmlAcceptHeader(String)</code></li>
                    <li><code>setImgAcceptHeader(String)</code></li>
                    <li><code>setScriptAcceptHeader(String)</code></li>
                    <li><code>setXmlHttpRequestAcceptHeader(String)</code></li>
                </ul>
            </subsection>

            <subsection name="WebClient level">
                <p>
                    To change request headers at the client level, use <code>WebClient.addRequestHeader()</code>.
                    You can add additional headers to every request made by this client or overwrite default ones.
                </p>
                <p>
                    Example: Add an additional header to every client request:
                </p>
                <source><![CDATA[client.addRequestHeader("from-htmlunit", "yes");]]></source>
                <p>
                    Example: Replace the default accept-language header for all requests made by this client:
                </p>
                <source><![CDATA[client.addRequestHeader(HttpHeader.ACCEPT_LANGUAGE, "fr");]]></source>
            </subsection>

            <subsection name="Request level">
                <p>
                    It is also possible to add or overwrite a request header for a specific request.
                </p>
<source><![CDATA[WebRequest wr = new WebRequest(URL_FIRST);
wr.setAdditionalHeader("from-htmlunit", "yes");

client.getPage(wr);
]]></source>
            </subsection>
        </section>

        <section name="Animations based on Window.requestAnimationFrame()" id="animations">
            <p>
                All browsers supported by HtmlUnit are able to run animations based on the
                <a href="https://developer.mozilla.org/en-US/docs/Web/API/window/requestAnimationFrame">Window.requestAnimationFrame() API</a>.
                A typical example for this is <a href="https://www.chartjs.org/">Chart.js</a>.
                Animation support is not triggered automatically because HtmlUnit runs headless.
                The JavaScript portion of the API is fully implemented, but users must explicitly trigger callback frames.
            </p>
            <p>
                Example:
            </p>
<source><![CDATA[try (final WebClient webClient = new WebClient(BrowserVersion.FIREFOX)) {
    HtmlPage page = webClient.getPage(uri);
    webClient.waitForBackgroundJavaScript(1_000);

    // page is loaded and async js done

    // process animation frames
    Window window = page.getEnclosingWindow().getScriptableObject();
    int i = 0; // limit animation cycles
    int pendingFrames = 0;
    do {
        i++;

        // force one animation cycle
        // invokes all animation callbacks registered for this window once
        pendingFrames = window.animateAnimationsFrames();
    } while (pendingFrames > 0 && i < 200);
}
]]></source>
            <p>
                This provides complete control over animation cycles—allowing you to skip frames or inspect page DOM state after each frame step.
            </p>
        </section>

        <section name="File download and Attachments" id="attachments">
            <p>
                Normally pages are loaded inline: clicking on a link loads the linked page in the current window. Attached pages are intended to be loaded outside of this flow: clicking on a link prompts the user to either save the file or open it externally without changing the active page.
            </p>
            <p>
                HtmlUnit complies with attachment semantics when an <code>AttachmentHandler</code> has been registered via <code>WebClient.setAttachmentHandler(AttachmentHandler)</code>. When no attachment handler is registered, attachments are loaded inline. By default, <code>AttachmentHandler</code>s are not registered on new <code>WebClient</code> instances.
            </p>
            <p>
                Please find more details and samples in the <a href="filedownload-howto.html">File download</a> section.
            </p>
        </section>

        <section name="Clipboard" id="clipboard">
            <p>
                Clipboard interaction is disabled by default for <code>WebClient</code>. This avoids side effects during test execution and removes dependencies on a running graphical desktop environment (Windows/X11/xvfb).
            </p>
            <p>
                To enable clipboard support, attach a clipboard handler to the <code>WebClient</code>. HtmlUnit provides <code>AwtClipboardHandler</code>
                for interacting with the system desktop clipboard (requires a graphical environment):
            </p>
<source><![CDATA[final ClipboardHandler clipboardHandler = new AwtClipboardHandler();
webClient.setClipboardHandler(clipboardHandler);
]]></source>
            <p>
                You can then control clipboard content programmatically:
            </p>
            <source><![CDATA[clipboardHandler.setClipboardContent("HtmlUnit");]]></source>
            <p>
                You can also implement a custom <code>ClipboardHandler</code> for full control
                or when running in headless environments.
            </p>
        </section>

        <section name="Content blocking" id="content_blocking">
            <p>
                Out of the box, HtmlUnit does not include default content blocking, but provides simple extension points
                to add custom blocking logic.
            </p>

            <subsection name="Blocking based on the request (URL)">
                <p>
                    This form of content blocking checks requested URLs against blocked lists or patterns. When blocked, the request is intercepted before hitting the network, returning a custom mock response.
                </p>
                <p>
                    Implement this with a <code>WebConnectionWrapper</code>:
                </p>
<source><![CDATA[try (WebClient webClient = new WebClient()) {
    webClient.getOptions().setThrowExceptionOnScriptError(false);

    // create a WebConnectionWrapper with a custom getResponse() impl
    new WebConnectionWrapper(webClient) {

        @Override
        public WebResponse getResponse(final WebRequest request) throws IOException {
            final URL url = request.getUrl();

            if (!isBlocked(url)) {
                return super.getResponse(request);
            }

            // construct alternative response
            final String content = "<html></html>";
            final WebResponseData data = new WebResponseData(content.getBytes(StandardCharsets.UTF_8),
                    200, "blocked", Collections.emptyList());
            final WebResponse blocked = new WebResponse(data, request, 0L);
            blocked.markAsBlocked("Blocked URL: '" + url.toExternalForm() + "'");
            return blocked;
        }

        private boolean isBlocked(final URL url) {
            return url.toExternalForm().contains("ad-server");
        }
    };

    final HtmlPage page = webClient.getPage(url);
}
]]></source>
            </subsection>

            <subsection name="Blocking based on the response (headers)">
                <p><em>Requires HtmlUnit 3.4.0 or later</em></p>
                <p>
                    Response-based blocking evaluates headers (such as <code>Content-Length</code>) before downloading
                    full response payloads, allowing early cancellation to improve test performance.
                </p>
                <p>
                    Subclass <code>HttpWebConnection</code> to intercept headers prior to downloading the response body:
                </p>
<source><![CDATA[try (WebClient webClient = new WebClient()) {
    webClient.getOptions().setThrowExceptionOnScriptError(false);

    webClient.setWebConnection(new HttpWebConnection(webClient) {
        @Override
        protected WebResponse downloadResponse(final HttpUriRequest httpMethod,
                final WebRequest webRequest, final HttpResponse httpResponse,
                final long startTime) throws IOException {

            final Header contentLengthHeader = httpResponse.getFirstHeader(HttpHeader.CONTENT_LENGTH);
            if (contentLengthHeader != null) {
                final int contentLength = Integer.parseInt(contentLengthHeader.getValue());

                // allow small downloads
                if (contentLength < 1_000) {
                    return super.downloadResponse(httpMethod, webRequest, httpResponse, startTime);
                }

                // abort body download for large content
                httpMethod.abort();

                // construct alternative response
                final String content = "<html></html>";
                final WebResponseData data = new WebResponseData(content.getBytes(StandardCharsets.UTF_8),
                        200, "blocked", Collections.emptyList());
                final WebResponse blocked = new WebResponse(data, webRequest, 0L);
                // if you like to check later on for blocked responses
                blocked.markAsBlocked("Blocked URL: '" + webRequest.getUrl().toExternalForm()
                            + "' content length: " + contentLength);
                return blocked;
            }

            return super.downloadResponse(httpMethod, webRequest, httpResponse, startTime);
        }
    });

    final HtmlPage page = webClient.getPage(url);
}
]]></source>
            </subsection>

            <subsection name="Blocking loading of frame content" id="framecontenthandler">
                <p>
                    By setting a custom <code>FrameContentHandler</code> on the <code>WebClient</code>, you can control
                    whether specific frame documents should be loaded.
                </p>
<source><![CDATA[try (WebClient webClient = new WebClient()) {
    webClient.setFrameContentHandler(new FrameContentHandler() {

        @Override
        public boolean loadFrameDocument(final BaseFrameElement baseFrameElement) {
            final String src = baseFrameElement.getSrcAttribute();
            // prevent loading frame content from external trackers
            return !src.contains("google");
        }

    });

    final HtmlPage page = webClient.getPage(url);
}
]]></source>
            </subsection>
        </section>

        <section name="Multithreading / Thread Pooling" id="multithreading">
            <p>
                HtmlUnit uses an <code>ExecutorService</code> backed by a <code>CachedThreadPool</code> for background thread
                management (such as WebSocket and asynchronous processing).
            </p>
            <p>
                You can provide a custom executor service using <code>WebClient.setExecutor(ExecutorService)</code> to manage
                thread pool sizing or apply custom thread naming conventions.
            </p>
        </section>

        <section name="Local / Session Storage" id="sessionstorage">
            <p>
                HtmlUnit supports the Web Storage API. The storage structures are accessible directly from <code>WebClient</code>
                using <code>StorageHolder</code> to inspect or pre-populate data before test execution.
            </p>

            <subsection name="SessionStorage example">
<source><![CDATA[try (WebClient webClient = new WebClient()) {

    // get session storage for current window
    final Map<String, String> sessionStorage =
            webClient.getStorageHolder().getSessionStorage(webClient.getCurrentWindow());

    // pre-populate session storage
    sessionStorage.put("myKey", "myData");

    // load page that consumes session storage
    webClient.getPage(url);

    assertEquals("myNewData", sessionStorage.get("myNewKey"));
}
]]></source>
            </subsection>

            <subsection name="LocalStorage example">
<source><![CDATA[try (WebClient webClient = new WebClient()) {

    // get local storage for target URL
    final Map<String, String> localStorage = webClient.getStorageHolder().getLocalStorage(url);

    // pre-populate local storage
    localStorage.put("myKey", "myData");

    webClient.getPage(url);

    assertEquals("myNewData", localStorage.get("myNewKey"));
}
]]></source>
            </subsection>
        </section>

        <section name="Client-side certificates" id="client-certificates">
            <p>
                HtmlUnit optionally supports client-side SSL/TLS certificates and custom trust stores configured via
                <code>WebClientOptions</code>:
            </p>
            <ul>
                <li><a href="/apidocs/org/htmlunit/WebClientOptions.html#setSSLClientCertificateKeyStore-java.io.InputStream-java.lang.String-java.lang.String-"><code>WebClientOptions.setSSLClientCertificateKeyStore(InputStream, String, String)</code></a></li>
                <li><a href="/apidocs/org/htmlunit/WebClientOptions.html#setSSLClientCertificate-java.net.URL-java.lang.String-java.lang.String-"><code>WebClientOptions.setSSLClientCertificateKeyStore(URL, String, String)</code></a></li>
                <li><a href="/apidocs/org/htmlunit/WebClientOptions.html#setSSLClientCertificateKeyStore-java.security.KeyStore-char:A-"><code>WebClientOptions.setSSLClientCertificateKeyStore(KeyStore, char[])</code></a></li>
                <li><a href="/apidocs/org/htmlunit/WebClientOptions.html#setSSLTrustStore-java.net.URL-java.lang.String-java.lang.String-"><code>WebClientOptions.setSSLTrustStore(URL, String, String)</code></a></li>
                <li><a href="/apidocs/org/htmlunit/WebClientOptions.html#setSSLClientProtocols-java.lang.String...-"><code>WebClientOptions.setSSLClientProtocols(String...)</code></a></li>
                <li><a href="/apidocs/org/htmlunit/WebClientOptions.html#setSSLInsecureProtocol-java.lang.String-"><code>WebClientOptions.setSSLInsecureProtocol(String)</code></a></li>
            </ul>
        </section>

    </body>
</document>