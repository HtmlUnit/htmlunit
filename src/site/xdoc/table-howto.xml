<?xml version="1.0"?>
<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://maven.apache.org/XDOC/2.0 https://maven.apache.org/xsd/xdoc-2.0.xsd">

    <properties>
        <title>Table Tutorial</title>
        <author email="mbowler@GargoyleSoftware.com">Mike Bowler</author>
        <author>RBRi</author>
    </properties>

    <body>

        <macro name="toc"/>

        <section name="Using Tables" id="use_tables">
            <p>
                The first set of examples uses this simple HTML table:
            </p>
            <source><![CDATA[<html><head><title>Table sample</title></head><body>
    <table id="table1">
        <tr>
            <th>Number</th>
            <th>Description</th>
        </tr>
        <tr>
            <td>5</td>
            <td>Bicycle</td>
        </tr>
    </table>
</body></html>]]></source>

            <p>
                This example demonstrates how to iterate over all rows and cells:
            </p>
            <source><![CDATA[final HtmlTable table = page.getHtmlElementById("table1");
for (final HtmlTableRow row : table.getRows()) {
    System.out.println("Found row");
    for (final HtmlTableCell cell : row.getCells()) {
        System.out.println("   Found cell: " + cell.asNormalizedText());
    }
}]]></source>

            <p>
                The following sample shows how to access specific cells by zero-based row and column indices:
            </p>
            <source><![CDATA[final WebClient webClient = new WebClient();
final HtmlPage page = webClient.getPage("http://foo.com");

final HtmlTable table = page.getHtmlElementById("table1");
// Access cell at row index 1 (second row) and column index 1 (second column)
final HtmlTableCell cell = table.getCellAt(1, 1);
System.out.println("Cell (1,1)=" + cell.asNormalizedText());]]></source>
        </section>

        <section name="Complex Tables" id="complex_tables">
            <p>
                The next examples use a more complex table containing header, footer, and body sections as well as a caption:
            </p>
            <source><![CDATA[<html><head><title>Table sample</title></head><body>
    <table id="table1">
        <caption>My complex table</caption>
        <thead>
            <tr>
                <th>Number</th>
                <th>Description</th>
            </tr>
        </thead>
        <tfoot>
            <tr>
                <td>7</td>
                <td></td>
            </tr>
        </tfoot>
        <tbody>
            <tr>
                <td>5</td>
                <td>Bicycle</td>
            </tr>
        </tbody>
        <tbody>
            <tr>
                <td>2</td>
                <td>Tricycle</td>
            </tr>
        </tbody>
    </table>
</body></html>]]></source>

            <p>
                <code>HtmlTableHeader</code>, <code>HtmlTableFooter</code>, and <code>HtmlTableBody</code> elements group rows together. 
                A table can contain at most one header and one footer, but may contain multiple body sections.
                Each section provides access to its rows via <code>getRows()</code>:
            </p>
            <source><![CDATA[final HtmlTableHeader header = table.getHeader();
final List<HtmlTableRow> headerRows = header.getRows();

final HtmlTableFooter footer = table.getFooter();
final List<HtmlTableRow> footerRows = footer.getRows();

for (final HtmlTableBody body : table.getBodies()) {
    final List<HtmlTableRow> rows = body.getRows();
    // process rows
}]]></source>

            <p>
                Tables may also optionally include a caption element describing their content:
            </p>
            <source><![CDATA[final String caption = table.getCaptionText();]]></source>
        </section>
    </body>
</document>