<?xml version="1.0"?>
<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://maven.apache.org/XDOC/2.0 https://maven.apache.org/xsd/xdoc-2.0.xsd">

    <properties>
        <title>HtmlUnit Coding Conventions</title>
        <author email="mbowler@GargoyleSoftware.com">Mike Bowler</author>
        <author>RBRi</author>
    </properties>

    <body>
        <macro name="toc"/>

        <section name="Introduction" id="introduction">
            <p>
                This page outlines the coding conventions used in HtmlUnit. All code contributions 
                to this project are expected to adhere to these conventions.
            </p>
            <p>
                The primary purpose of shared coding conventions is to ensure consistency, legibility, 
                and maintainability across the codebase. The guidelines detailed in this document reflect best practices 
                adopted to keep the codebase robust over time.
            </p>
            <p>
                We use <a href="https://checkstyle.sourceforge.io">Checkstyle</a> to enforce coding standard rules automatically. 
                Rules enforced by Checkstyle are only documented below if they require additional explanation. 
                You can run Checkstyle using Maven:
            </p>
            <source>mvn checkstyle:checkstyle</source>
        </section>

        <section name="Unit Tests" id="unit_tests">
            <p>
                All code must have thorough test coverage built with the <a href="https://junit.org">JUnit</a> 
                testing framework. Writing code test-first is strongly encouraged. 
                Execute tests with the following Maven target:
            </p>
            <source>mvn test</source>
        </section>

        <section name="Abbreviations" id="abbreviations">
            <p>
                Abbreviations obscure intent and should generally be avoided in variable and method names. 
                Explicit, descriptive naming is preferred to ensure self-documenting code.
            </p>
            <p>
                The following table lists the only universally permitted abbreviations. 
                Any abbreviation not included in this list is not allowed:
            </p>
            <table>
                <thead>
                    <tr>
                        <td>Abbreviation</td>
                        <td>Description</td>
                        <td>Comments</td>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Util</td>
                        <td>Utility</td>
                        <td>Used only in class names (e.g., <code>StringUtil</code>).</td>
                    </tr>
                    <tr>
                        <td>e</td>
                        <td>Exception</td>
                        <td>Used only in catch blocks (e.g., <code>catch (MyException e)</code>).</td>
                    </tr>
                    <tr>
                        <td>i</td>
                        <td>Loop index variable</td>
                        <td>Used in standard loop iterations.</td>
                    </tr>
                </tbody>
            </table>
            <p>
                To propose adding a new abbreviation to this list, start a thread on the developer mailing list. 
                If no maintainers object, it will be added to the convention guidelines.
            </p>
        </section>

        <section name="The final Keyword" id="the_final_keyword">
            <p>
                The <code>final</code> keyword should be applied wherever possible. 
                It communicates intent clearly to developers and the compiler, helping prevent unintended reassignments 
                and identifying potential state errors early.
            </p>

            <p>
                Declare variables <code>final</code> to indicate they are assigned only once:
            </p>
            <source>private final int fooCount = 12;</source>

            <p>
                Declare method parameters <code>final</code> to prevent accidental parameter reassignment:
            </p>
            <source>public void setFooList(final List&lt;Foo&gt; fooList) {</source>

            <p>
                Declare methods <code>final</code> to indicate they cannot be overridden in subclass implementations:
            </p>
            <source>public final int getFooCount() {</source>

            <p>
                Declare classes <code>final</code> when they are not designed for inheritance:
            </p>
            <source>public final class FooCounter {</source>
        </section>

        <section name="Import Statements" id="import_statements">
            <p>
                Use explicit, fully qualified import statements instead of wildcard imports:
            </p>
            <source><![CDATA[import java.util.List;
import java.util.ArrayList;]]></source>

            <p>
                Do not use wildcard imports:
            </p>
            <source>import java.util.*;</source>

            <p>
                Explicit imports make dependencies transparent when reading and reviewing code. 
                IDEs like Eclipse and IntelliJ IDEA can be configured to manage imports and prune unused ones automatically.
            </p>
        </section>

        <section name="Indentation" id="indentation">
            <p>
                Indent code using spaces instead of tabs. Use four (4) spaces per indentation level.
            </p>
        </section>

        <section name="Deprecation" id="deprecation">
            <p>
                When public API elements must be retired, deprecate the API using the JavaDoc <code>@deprecated</code> tag. 
                Deprecated code must remain in the codebase for at least 6 months and across at least 2 major/minor releases.
            </p>
            <p>
                When direct deprecation is not feasible, log the change in the <a href="changes-report.html">Change Log</a> 
                flagged with <code>INCOMPATIBLE CHANGE</code> to make breaking updates explicit.
            </p>
        </section>
    </body>
</document>