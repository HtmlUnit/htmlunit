<?xml version="1.0"?>
<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://maven.apache.org/XDOC/2.0 https://maven.apache.org/xsd/xdoc-2.0.xsd">

    <properties>
        <title>Details about HtmlUnit file download</title>
        <author>RBRi</author>
    </properties>

    <body>

        <macro name="toc"/>

        <section name="File downloads in real browsers" id="real_browsers">
            <p>
                Because file downloading can be complex, let's start with some background info about file downloads in
                real browsers.
            </p>
            <p>
                When surfing the internet, you usually open URLs pointing to (X)HTML content. Your web browser parses this data
                and renders the content on your screen. There are also additional file types your browser handles natively,
                such as CSS, JavaScript, images, sound, and video files. This processing occurs automatically behind the scenes.
            </p>
            <p>
                There are only three primary cases where a browser delegates control of content handling to the user:
            </p>
            <ul>
                <li>The file type (MIME type) is not supported natively by your browser (e.g., an Excel or Word file).</li>
                <li>The <a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Disposition">Content-Disposition Header</a>
                of the HTTP response explicitly marks the content as an <strong>attachment</strong>.</li>
                <li>The request is initiated from the client side by clicking an anchor element with the
                <a href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLAnchorElement/download"><code>download</code></a> attribute set,
                prompting local file storage.</li>
            </ul>
            <p>
                In all three cases, the content is saved as a file on your local disk. Depending on user settings, this occurs automatically
                or by displaying a file save dialog.
            </p>
        </section>

        <section name="File downloads with HtmlUnit" id="htmlunit_downloads">
            <p>
                Due to HtmlUnit's nature as a headless browser, there is no GUI rendering. However, like real browsers,
                HtmlUnit parses pages delivered by the server. If the response type is supported, the content is made available
                as an <code>HtmlPage</code> (or <code>XHtmlPage</code>), providing full programmatic access to embedded elements
                like JavaScript, CSS, and images. HtmlUnit also provides support for text-only content (<code>TextPage</code>)
                and plain XML content (<code>XmlPage</code>).
            </p>
            <p>
                Because HtmlUnit is primarily designed for automated testing and scraping, it cannot present a graphical
                file dialog. The current implementation offers two primary mechanisms to handle file downloads:
            </p>

            <subsection name="UnexpectedPage (default)">
                <p>
                    HtmlUnit handles unknown or unsupported content types similarly to known content—the data stream is
                    wrapped inside a page object and placed within a window. For unhandled file types, HtmlUnit creates
                    an <code>UnexpectedPage</code> instance. In most cases, <code>UnexpectedPage</code> replaces
                    the current page in the active window, though in certain cases (such as clicking an anchor with
                    a <code>download</code> attribute), a new target window is created.
                </p>
                <p>
                    You can access the raw downloaded content stream directly from the enclosed <code>UnexpectedPage</code>:
                </p>
<source><![CDATA[try (final WebClient webClient = new WebClient(BrowserVersion.FIREFOX)) {
    HtmlPage page = webClient.getPage(uri);
    WebWindow window = page.getEnclosingWindow();

    // click an anchor/button that triggers a file download

    UnexpectedPage downloadPage = (UnexpectedPage) window.getEnclosedPage();

    try (InputStream downloadedContent = downloadPage.getInputStream()) {
        // e.g., save input stream to a local file
    }
}
]]></source>

                <p>
                    In cases where the download opens inside a new window, you can retrieve the window instance like this:
                </p>
                <source><![CDATA[WebWindow newWindow = webClient.getWebWindows().get(webClient.getWebWindows().size() - 1);]]></source>
            </subsection>

            <subsection name="AttachmentHandler">
                <p>
                    To customize default download handling, you can register a custom implementation of the <code>AttachmentHandler</code> interface
                    on the <code>WebClient</code> instance.
                </p>
                <p>
                    The <code>AttachmentHandler</code> interface works alongside default <code>UnexpectedPage</code> handling.
                    If your custom <code>AttachmentHandler</code> declines a response, HtmlUnit falls back to the default behavior
                    and assigns an <code>UnexpectedPage</code> to the window. You can override the <code>isAttachment()</code> method
                    in your handler to target specific responses. By default, <code>isAttachment()</code> detects responses
                    with a <code>Content-Disposition</code> header of type <code>attachment</code>, or responses without
                    a <code>Content-Disposition</code> header that have a <code>Content-Type</code> of <code>application/octet-stream</code>.
                </p>
                <p>
                    The <code>handleAttachment(WebResponse, String)</code> method is triggered when a response is identified as
                    an attachment:
                </p>
                <source><![CDATA[boolean handleAttachment(final WebResponse response, final String attachmentFilename)]]></source>
                <p>
                    You can process the attachment in your implementation (for example, saving it directly to a file system)
                    and return a boolean indicating whether the event was handled. Depending on the return value, the handler
                    operates in two modes:
                </p>
                <ol>
                    <li>
                        <strong><code>true</code></strong>: Signals that the response has been fully handled.
                        The current page will <strong>NOT</strong> be replaced by an <code>UnexpectedPage</code>,
                        and <code>handleAttachment(Page, String)</code> will not be called.
                    </li>
                    <li>
                        <strong><code>false</code></strong>: Delegates further processing back to HtmlUnit:
                        <ul>
                            <li>A new web window is created.</li>
                            <li>An <code>UnexpectedPage</code> is constructed.</li>
                            <li>The method <code>handleAttachment(Page, String)</code> on your <code>AttachmentHandler</code>is invoked.</li>
                            <li>The <code>UnexpectedPage</code> is placed inside the new window.</li>
                        </ul>
                    </li>
                </ol>

                <p>
                    The following example collects attachment responses into a list without replacing the active window content:
                </p>
<source><![CDATA[final List<WebResponse> attachments = new ArrayList<>();

try (final WebClient webClient = new WebClient(BrowserVersion.FIREFOX)) {

    webClient.setAttachmentHandler(new AttachmentHandler() {
        @Override
        public boolean handleAttachment(final WebResponse response, final String attachmentFilename) {
            attachments.add(response);
            return true;
        }

        @Override
        public void handleAttachment(final Page page, final String attachmentFilename) {
            throw new IllegalAccessError("handleAttachment(Page, String) called unexpectedly");
        }
    });

    // start browsing
    HtmlPage page = webClient.getPage(uri);
}
]]></source>
                <p>
                    For additional details, refer to the <code>AttachmentHandler</code> JavaDoc API documentation.
                </p>
                <p>
                    <em>Note on special cases:</em> Clicking an anchor tag with the <code>download</code> attribute
                    set bypasses <code>isAttachment()</code> checks and directly forwards the response to the registered
                    <code>AttachmentHandler</code>.
                </p>
            </subsection>
        </section>
    </body>
</document>