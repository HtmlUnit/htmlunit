<?xml version="1.0"?>
<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://maven.apache.org/XDOC/2.0 https://maven.apache.org/xsd/xdoc-2.0.xsd">

    <properties>
        <title>Windows and Frames</title>
        <author email="mbowler@GargoyleSoftware.com">Mike Bowler</author>
        <author>Ahmed Ashour</author>
        <author>RBRi</author>
    </properties>

    <body>
        <macro name="toc"/>

        <section name="WebWindow" id="web_window">
            <p>
                All pages are contained within <code>WebWindow</code> objects. This could be a
                <code>TopLevelWindow</code> representing an actual browser window, an
                <code>HtmlFrame</code> representing a <code>&lt;frame&gt;</code> element, or an
                <code>HtmlInlineFrame</code> representing an <code>&lt;iframe&gt;</code> element.
            </p>
            <p>
                When a <code>WebClient</code> is first instantiated, a <code>TopLevelWindow</code> is automatically created.
                You can think of this as the first window displayed by a web browser. Calling
                <code>WebClient.getPage(WebWindow, WebRequest)</code>
                will load the new page into this window.
                <br/>
                Closing the last <code>TopLevelWindow</code> of a <code>WebClient</code> will automatically open a new empty window. 
            </p>
            <p>
                The JavaScript <code>open()</code> function can be used to load pages into other windows. New <code>WebWindow</code>
                objects will be created automatically by this function.
            </p>
        </section>

        <section name="WebWindowEvents" id="web_window_events">
            <p>
                If you wish to be notified when windows are created or pages are loaded, register a
                <a href="apidocs/org/htmlunit/WebWindowListener.html">WebWindowListener</a> with the <code>WebClient</code> via
                <code>WebClient.addWebWindowListener(WebWindowListener)</code>.
            </p>
            <p>
                When a window is opened either by JavaScript or through the <code>WebClient</code>, a <code>WebWindowEvent</code> will be
                fired and passed into the <code>WebWindowListener.webWindowOpened(WebWindowEvent)</code> method. 
                Note that both the new and old pages in the event will be <code>null</code> because the window does not
                have any content loaded at this point. If a URL was specified during creation of the window, the page
                will be loaded and another event will be fired.
            </p>
            <p>
                When a new page is loaded into a specific window, a <code>WebWindowEvent</code> is fired and passed into the
                <code>WebWindowListener.webWindowContentChanged(WebWindowEvent)</code> method.
            </p>
            <p>
                When an existing window is closed, <code>WebWindowListener.webWindowClosed(WebWindowEvent)</code> is fired.
            </p>
        </section>

        <section name="Frame Example I" id="frame_example_1">
            <p>
                Getting the page inside a <code>&lt;frame&gt;</code> or <code>&lt;iframe&gt;</code> element can be done by using
                <code>HtmlPage.getFrames()</code>.
                <br/>
                Suppose you have the following page:
            </p>
            <source><![CDATA[<html>
  <body>
    <iframe src="two.html">
  </body>
</html>]]></source>
            <p>
                You can use the following code to get the content of <code>two.html</code>:
            </p>
            <source><![CDATA[final List<FrameWindow> windows = page.getFrames();
final HtmlPage pageTwo = (HtmlPage) windows.get(0).getEnclosedPage();]]></source>
        </section>

        <section name="Frame Example II" id="frame_example_2">
            <p>
                Here is an example that navigates API documentation to retrieve a desired class page:
            </p>
            <source><![CDATA[final WebClient client = new WebClient();
final HtmlPage mainPage = client.getPage("https://www.htmlunit.org/apidocs/index.html");]]></source>

            <p>
                To get the page of the first frame (upper-left) and click the sixth link:
            </p>
            <source><![CDATA[final HtmlPage packageListPage = (HtmlPage) mainPage.getFrames().get(0).getEnclosedPage();
packageListPage.getAnchors().get(5).click();]]></source>

            <p>
                To get the page of the frame named <code>'packageFrame'</code> (lower-left) and click the second link:
            </p>
            <source><![CDATA[final HtmlPage packagePage = (HtmlPage) mainPage.getFrameByName("packageFrame").getEnclosedPage();
packagePage.getAnchors().get(1).click();]]></source>
        
            <p>
                To get the page of the frame named <code>'classFrame'</code> (right):
            </p>
            <source><![CDATA[final HtmlPage classPage = (HtmlPage) mainPage.getFrameByName("classFrame").getEnclosedPage();]]></source>
        </section>
    </body>
</document>