This directory contains the resources necessary to run the MooTools (http://mootools.net/) JS tests.

Each subdirectory corresponds to a specific version. Each of these subdirectories was created by
cloning the appropriate GIT tag from GitHub.
