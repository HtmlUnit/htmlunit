import { WebDriver, Capabilities } from "selenium-webdriver";
import { Options, RemoteTestRunResult } from "./MochaRemoteRunner";
export declare function withWebDriver<T>(capabilities: Object | Capabilities, test: (driver: WebDriver) => T): Promise<T>;
/**
 * Run Mocha tests using `webDriver` (instance or `Capabilities` used to build instance).
 */
export declare function runMochaWebDriverTest(webDriver: WebDriver | Capabilities, url: string, options?: Options): Promise<RemoteTestRunResult>;
