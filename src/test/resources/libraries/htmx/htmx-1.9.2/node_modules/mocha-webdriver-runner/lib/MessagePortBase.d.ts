export declare abstract class MessagePortBase {
    private emitter;
    started: boolean;
    abstract postMessage(message: any): void;
    abstract doReceiveRemoteEvents(): Promise<void>;
    start(): void;
    close(): void;
    set onmessage(listener: (event: any) => void);
    set onmessageerror(listener: (event: any) => void);
    addEventListener(type: "message" | "error", listener: (event: any) => void): void;
    removeEventListener(type: "message" | "error", listener: (event: any) => void): void;
    dispatchEvent(event: Event): boolean;
    private receiveRemoteEvents;
}
