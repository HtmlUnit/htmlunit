This directory contains the resources necessary to run the YUI JavaScript library unit tests.

Each subdirectory corresponds to a specific version of the YUI library. Each of these subdirectories
was created by downloading the distribution and copying the build and tests directories.
